import SwiftUI

/// Forge's default dark-card palette, shared by metrics and status states.
enum ForgeWidgetStyle {
    static let card = Color(red: 15 / 255, green: 15 / 255, blue: 18 / 255)
    static let elevated = Color(red: 10 / 255, green: 10 / 255, blue: 10 / 255)
    static let border = Color(red: 31 / 255, green: 31 / 255, blue: 36 / 255)
    static let foreground = Color.white
    static let muted = Color(red: 161 / 255, green: 161 / 255, blue: 170 / 255)
}
